/* You are given a number stored in a variable, with the name number.
Multiply the number stored in a the variable number by 50, and print the 
result obtained.
*/
function multiplyBy50(number) {
    number = number * 50;
    console.log(number);
}
